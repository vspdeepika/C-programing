 #include<stdio.h>
int main()
{
char str[100];
gets(str);
int i,count=0,num=0,upper=0;
for(i=0;str[i]!='\0';i++)
{
	if(islower(str[i]))
	{ 
	count++;
	}
		if(isupper(str[i]))
	{ 
	upper++;
	}
		if(isdigit(str[i]))
	{ 
	num++;
	}
}printf("%d",upper,"%d",count,"%d",num);
}
